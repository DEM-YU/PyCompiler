# fibonacci.nv  — recursive Fibonacci, from the project specification

fn fib(n: int) -> int {
    if n <= 1 { return n; }
    return fib(n - 1) + fib(n - 2);
}

fn main() {
    let result: int = fib(10);
    print(result);
    return 0; 
}
